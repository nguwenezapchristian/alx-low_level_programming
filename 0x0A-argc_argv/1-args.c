#include "main.h"
#include <stdio.h>

/**
 * main - print number of arguments
 * @argc: argument count
 * @argv: array argument
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
