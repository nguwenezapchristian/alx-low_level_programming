#include <stdio.h>
/**
 * main - Entry point
 * Description: 'lowercase in reverse'
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* decaralation */
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	/* return zero */
	return (0);
}
