#include "main.h"
/**
 * mul - entry point
 * @a: number
 * @b: number to multiply with
 * description: 'multiply two numbers'
 * Return: sum
 */
int mul(int a, int b)
{
	int sum = a * b;

	return (sum);
}
