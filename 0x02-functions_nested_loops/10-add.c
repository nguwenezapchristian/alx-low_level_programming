#include "main.h"
/**
 * add - entry point
 * @num1: integer one
 * @num2: integer two
 * description: 'adds two number'
 * Return: Always return sum
 */
int add(int num1, int num2)
{
	int sum = num1 + num2;

	return (sum);
}
